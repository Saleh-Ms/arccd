let express,dotenv,connectDB;_2f0‍.w("express",[["default",["express"],function(v){express=v}]]);_2f0‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_2f0‍.w("./config/db",[["default",["connectDB"],function(v){connectDB=v}]]);



dotenv.config()
connectDB()
const app = express();
app.use(express.json())

app.get('/', (req, res) => {
    res.send("server is running...")
})

const PORT = process.env.PORT || 5000

app.listen(PORT, () => console.log(`server is running on port ${PORT}`) )